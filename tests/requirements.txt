# Core API
fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.7.0
python-dotenv==1.0.1

# Rate limiting
slowapi==0.1.9

# ML
vaderSentiment==3.3.2
transformers==4.41.0
torch==2.3.0
langdetect==1.0.9

# Database (Phase 2)
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0          # async PostgreSQL driver
alembic==1.13.1          # database migrations

# Redis (Phase 2)
redis==5.0.4

# Security (Phase 2)
cryptography==42.0.7
python-jose[cryptography]==3.3.0   # JWT
passlib[bcrypt]==1.7.4

# HTTP client for Meta API calls (Phase 2)
httpx==0.27.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
httpx==0.27.0            # TestClient uses this
